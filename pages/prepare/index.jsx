import { MainLayOut } from "@/components"

const PreparePage = () => {
  return (
    <MainLayOut title={`รายการจัดเตรียม`} description={`จัดการข้อมูลการจัดเตรียมสินค้า`}>
      <h1>Receive Page</h1>
    </MainLayOut>
  )
}

export default PreparePage