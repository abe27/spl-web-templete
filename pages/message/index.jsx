import { MainLayOut } from "@/components"

const MessagePage = () => {
  return (
    <MainLayOut title={`ข้อความส่วนตัว`} description={`จัดการข้อมูลข้อความส่วนตัว`}>
      <h1>Receive Page</h1>
    </MainLayOut>
  )
}

export default MessagePage